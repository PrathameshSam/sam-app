library identifier: 'samsharedlib@master', retriever: modernSCM(
  [$class: 'GitSCMSource',
   remote: 'https://github.com/PrathameshSam/prathamesh-sam-shared-library.git'])
pipeline{
    agent any
    parameters{
        choice(name: 'goal', choices: ['install', 'package', 'test'], description: "Select a Goal")
        // #choice(name: 'crawler_env', choices: ['install', 'package', 'test'], description:  'Select an Environment')
    }
    stages{
    stage('Git Checkout') {
    githubScm(
        branch: "master",
        url: "https://github.com/PrathameshSam/sam-app.git"
    )
}
}
}
